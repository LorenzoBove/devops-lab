services:

  mongodb:
    image: mongo:7
    container_name: devops-lab-mongodb
    restart: unless-stopped

    volumes:
      - mongodb-data:/data/db

    networks:
      - devops-lab

    healthcheck:
      test:
        [
          "CMD",
          "mongosh",
          "--quiet",
          "--eval",
          "db.adminCommand('ping').ok"
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s


  api:
    image: ${IMAGE_NAME}:${IMAGE_TAG}
    container_name: devops-lab-api
    restart: unless-stopped

    environment:
      MONGODB_URL: mongodb://mongodb:27017
      MONGODB_DATABASE: devops_lab

    expose:
      - "8000"

    depends_on:
      mongodb:
        condition: service_healthy

    networks:
      - devops-lab

    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s


  caddy:
    image: caddy:2-alpine
    container_name: devops-lab-caddy
    restart: unless-stopped

    ports:
      - "80:80"

    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro

    depends_on:
      api:
        condition: service_healthy

    networks:
      - devops-lab


volumes:

  mongodb-data:
    external: true
    name: devops-lab-mongodb-data


networks:

  devops-lab: